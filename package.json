{
  "name": "lava-timer",
  "private": true,
  "version": "1.0.0",
  "description": "A lightweight macOS menu bar focus timer built with Tauri.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "test:frontend": "node --test tests/frontend-state.test.mjs",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml",
    "check": "npm run test:frontend && npm run test:rust",
    "info": "tauri info",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
