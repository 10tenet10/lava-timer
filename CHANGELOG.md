# Changelog

## 1.0.3 — 2026-08-31

### 修复

- 修复面板滚动后切换胶囊时保留旧滚动位置、导致内容被裁切的问题
- 状态栏暂停时显示 `Lava` 标识，并保留 Dock 入口作为恢复窗口的双保险
- 为面板、总览、胶囊和设置界面增加明确的关闭窗口按钮
- 点击 Dock 图标可重新显示已隐藏的窗口

## 1.0.2 — 2026-08-31

### 修复

- 应用启动时直接显示主面板，避免进程运行但没有可见入口
- 在 `Info.plist` 中声明菜单栏应用模式，确保不依赖 Dock 图标唤回窗口
- 将前端运行时随应用本地打包，修复安全策略阻止模板渲染的问题

## 1.0.1 — 2026-08-31

### 修复

- 修复正式安装包遗漏菜单栏图标资源，导致应用在初始化阶段退出的问题
- 将菜单栏图标嵌入二进制作为兜底，避免资源清单异常再次阻止应用启动
- 增加打包资源回归检查

## 1.0.0 — 2026-08-30

LavaTimer 的首个稳定版本。

### 主要功能

- 多项目独立计时与每日目标
- 完整面板、胶囊模式，以及可切换 7 天 / 30 天 / 全部记录的历史总览
- 菜单栏计时、透明悬浮窗口与屏幕边缘吸附
- 锁屏、休眠、熄屏和屏幕保护启动时自动暂停
- 本地历史记录和跨午夜时间拆分

### 稳定性

- 应用退出后只结算到最后一次存活记录，避免把离线时间计入专注
- 菜单栏计时改由原生线程刷新，避免 WebView 隐藏时被系统节流
- 删除项目和重置当日计时前增加确认
- 增加前端状态与 Rust 窗口/托盘逻辑测试

### 已知限制

- 预构建版本仅提供 Apple Silicon 架构
- 发布包使用 ad-hoc 签名，尚未经过 Apple notarization
- 记录仅保存在本机，不提供云同步或自动备份
